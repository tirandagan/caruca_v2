# Parity diff — `uniq`

What v1 produced, what v2 produced, and where they differ. Generated by `scripts/parity_diff.py`; the aggregates are in `ai_docs/analysis/v1_v2_parity_study.md`.

v1 commit `d8032407346aadc135b14c043618c8c1d4f4e0cf` · model `openai/gpt-4o` · temperature 0.0 · bound `--max-arity 1 --max-count 1`

> Quotes v1 source verbatim. v1 is private and unlicensed — read locally, do not commit or share.

---

## Stage 1 — syntax specification
* ground truth: `/Users/tirandagan/dev/stevens/caruca/caruca/src/caruca/syntax_specs/uniq.py`
* v1's own LLM output: `/Users/tirandagan/dev/stevens/caruca/outputs/llm-dsl-generation/uniq.py`
* v2's output: `eval/runs/2026-09-14T130857Z_uniq_aea80dd1/uniq.py`

### v1 LLM output — scored against ground truth

* matched 13 of 13 reference flags; missing 0, spurious 0
* fully agreeing arguments: 13 of 13 (F1 1.000, exact 1.000)

### v2 output — scored against ground truth

* matched 13 of 13 reference flags; missing 0, spurious 0
* fully agreeing arguments: 13 of 13 (F1 1.000, exact 1.000)

### v1's LLM output vs v2's, line by line

```diff
--- v1
+++ v2
@@ -27,8 +27,4 @@
         ],
-        [
-            Path(arity=Arity.ZERO_OR_ONE),  # INPUT
-            Path(arity=Arity.ZERO_OR_ONE),  # OUTPUT
-        ],
+        [Path(arity=Arity.ZERO_OR_ONE), Path(arity=Arity.ZERO_OR_ONE)],
     ]
 ]
-
```
---

## Stage 2 — configuration generation

* v1 emitted **43** lines (**27** distinct invocations)
* v2 emitted **25** lines (**25** distinct)
* matched **0**, missing **27**, spurious **25** (recall 0.0, precision 0.0)
* equivalence: options compared as a multiset of (flag, value) — order-insensitive; operands compared as an ordered sequence; '=' joining, attached short values, and short bundles normalized; shell quoting removed by shlex

### What v1 emitted

```text
uniq
uniq --all-repeated=none
uniq --all-repeated=prepend
uniq --all-repeated=separate
uniq --group=append
uniq --group=both
uniq --group=prepend
uniq --group=separate
uniq --help
uniq --version
uniq -D
uniq -c
uniq -d
uniq -f=-1
uniq -f=0
uniq -f=1
uniq -i
uniq -s=-1
uniq -s=0
uniq -s=1
uniq -u
uniq -w=-1
uniq -w=0
uniq -w=1
uniq -z
uniq abspath_1
uniq relpath_1
```

### What v2 emitted

```text
uniq --all-repeated=none relpath_1 abspath_1
uniq --all-repeated=prepend relpath_1 abspath_1
uniq --all-repeated=separate relpath_1 abspath_1
uniq --group=append relpath_1 abspath_1
uniq --group=both relpath_1 abspath_1
uniq --group=prepend relpath_1 abspath_1
uniq --group=separate relpath_1 abspath_1
uniq --help relpath_1 abspath_1
uniq --version relpath_1 abspath_1
uniq -D relpath_1 abspath_1
uniq -c relpath_1 abspath_1
uniq -d relpath_1 abspath_1
uniq -f=-1 relpath_1 abspath_1
uniq -f=0 relpath_1 abspath_1
uniq -f=1 relpath_1 abspath_1
uniq -i relpath_1 abspath_1
uniq -s=-1 relpath_1 abspath_1
uniq -s=0 relpath_1 abspath_1
uniq -s=1 relpath_1 abspath_1
uniq -u relpath_1 abspath_1
uniq -w=-1 relpath_1 abspath_1
uniq -w=0 relpath_1 abspath_1
uniq -w=1 relpath_1 abspath_1
uniq -z relpath_1 abspath_1
uniq relpath_1 abspath_1
```

### In v1 but not v2 (missing) — 20 shown

```text
uniq
uniq --all-repeated=none
uniq --all-repeated=prepend
uniq --all-repeated=separate
uniq --group=append
uniq --group=both
uniq --group=prepend
uniq --group=separate
uniq --help
uniq --version
uniq -D
uniq -c
uniq -d
uniq -f=-1
uniq -f=0
uniq -f=1
uniq -i
uniq -s=-1
uniq -s=0
uniq -s=1
```

### In v2 but not v1 (spurious) — 20 shown

```text
uniq --all-repeated=none relpath_1 abspath_1
uniq --all-repeated=prepend relpath_1 abspath_1
uniq --all-repeated=separate relpath_1 abspath_1
uniq --group=append relpath_1 abspath_1
uniq --group=both relpath_1 abspath_1
uniq --group=prepend relpath_1 abspath_1
uniq --group=separate relpath_1 abspath_1
uniq --help relpath_1 abspath_1
uniq --version relpath_1 abspath_1
uniq -D relpath_1 abspath_1
uniq -c relpath_1 abspath_1
uniq -d relpath_1 abspath_1
uniq -f=-1 relpath_1 abspath_1
uniq -f=0 relpath_1 abspath_1
uniq -f=1 relpath_1 abspath_1
uniq -i relpath_1 abspath_1
uniq -s=-1 relpath_1 abspath_1
uniq -s=0 relpath_1 abspath_1
uniq -s=1 relpath_1 abspath_1
uniq -u relpath_1 abspath_1
```

### Form differences among matches

Of 0 matched invocations, 0 were spelled identically. 0 differed in `=` style and 0 in token order only — reported, never scored.

---

## Stage 3 — execution and tracing

* 0 of 3 cells produced a usable observation
* in the first cell, 0 of 5 sessions called `report_observations`; the rest ended in prose after 2 turns

_no v2 traces to compare_

---

## Stage 4 — annotation

* v1: `/Users/tirandagan/dev/stevens/caruca/caruca/outputs/uniq.parity.pash.json`
* v2: `eval/runs/2026-09-14T155943Z_uniq_f3d2ec1d/uniq.pash.annotation`
* hand-curated ground truth

### v1's annotator

12 case(s)

```json
[
 {
  "predicate": {
   "operator": "and",
   "operands": [
    {
     "operator": "exists",
     "operands": []
    },
    {
     "operator": "len_args_eq",
     "operands": [
      0
     ]
    }
   ]
  },
  "pclass": "non-pure",
  "inputs": [
   "stdin"
  ],
  "outputs": [
   "stdout"
  ],
  "true_str": "uniq",
  "comments": "Automatically generated by caruca."
 },
 {
  "predicate": {
   "operator": "and",
   "operands": [
    {
     "operator": "exists",
     "operands": [
      "-d"
     ]
    },
    {
     "operator": "len_args_eq",
     "operands": [
      0
     ]
    }
   ]
  },
… 280 more
```

### v2

2 case(s)

```json
[
 {
  "predicate": "default",
  "pclass": "pure",
  "inputs": [
   "stdin"
  ],
  "outputs": [
   "stdout"
  ],
  "true_str": "uniq",
  "comments": "The command reads from stdin and writes to stdout. It is pure as it does not modify any files or directories."
 },
 {
  "predicate": {
   "operator": "exists",
   "operands": [
    "PATH_JYTVH"
   ]
  },
  "pclass": "pure",
  "inputs": [
   "PATH_JYTVH"
  ],
  "outputs": [
   "stdout"
  ],
  "true_str": "uniq relpath_1",
  "comments": "When a file path is provided, uniq reads from the file and writes to stdout. It is pure as it does not modify any files or directories."
 }
]
```

### hand-curated ground truth

1 case(s)

```json
[
 {
  "predicate": "default",
  "class": "pure",
  "inputs": [
   "stdin"
  ],
  "outputs": [
   "stdout"
  ]
 }
]
```

### v2 against the ground truth

* alignment: `predicate_key` (the two are written at different granularities)
* aligned 1 case(s); class agreeing 1, fully agreeing 1


---

