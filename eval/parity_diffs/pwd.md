# Parity diff — `pwd`

What v1 produced, what v2 produced, and where they differ. Generated by `scripts/parity_diff.py`; the aggregates are in `ai_docs/analysis/v1_v2_parity_study.md`.

v1 commit `d8032407346aadc135b14c043618c8c1d4f4e0cf` · model `openai/gpt-4o` · temperature 0.0 · bound `--max-arity 1 --max-count 1`

> Quotes v1 source verbatim. v1 is private and unlicensed — read locally, do not commit or share.

---

## Stage 1 — syntax specification
* ground truth: `/Users/tirandagan/dev/stevens/caruca/caruca/src/caruca/syntax_specs/pwd.py`
* v1's own LLM output: `/Users/tirandagan/dev/stevens/caruca/outputs/llm-dsl-generation/pwd.py`
* v2's output: `eval/runs/2026-09-14T130812Z_pwd_7de147c1/pwd.py`

### v1 LLM output — scored against ground truth

* matched 4 of 4 reference flags; missing 0, spurious 0
* fully agreeing arguments: 4 of 4 (F1 1.000, exact 1.000)

### v2 output — scored against ground truth

* matched 4 of 4 reference flags; missing 0, spurious 0
* fully agreeing arguments: 4 of 4 (F1 1.000, exact 1.000)

### v1's LLM output vs v2's, line by line

```diff
--- v1
+++ v2
@@ -10,5 +10,4 @@
         ],
-        [],
+        []
     ]
 ]
-
```
---

## Stage 2 — configuration generation

* v1 emitted **8** lines (**5** distinct invocations)
* v2 emitted **7** lines (**7** distinct)
* matched **5**, missing **0**, spurious **2** (recall 1.0, precision 0.7142857142857143)
* equivalence: options compared as a multiset of (flag, value) — order-insensitive; operands compared as an ordered sequence; '=' joining, attached short values, and short bundles normalized; shell quoting removed by shlex

### What v1 emitted

```text
pwd
pwd --help
pwd --version
pwd -L
pwd -P
```

### What v2 emitted

```text
pwd
pwd --help
pwd --logical
pwd --physical
pwd --version
pwd -L
pwd -P
```

### In v1 but not v2 (missing) — 0 shown

_none_


### In v2 but not v1 (spurious) — 2 shown

```text
pwd --logical
pwd --physical
```

### Form differences among matches

Of 5 matched invocations, 5 were spelled identically. 0 differed in `=` style and 0 in token order only — reported, never scored.

---

## Stage 3 — execution and tracing

* 3 of 3 cells produced a usable observation
* in the first cell, 4 of 5 sessions called `report_observations`; the rest ended in prose after 2 turns

* v1 recorded 36 raw `(action, path)` pairs, reduced to **2 distinct** by v1's own relevance filter (28 system paths dropped)
* core recall 1.0, inference recall 0.0

* v1 traces: `/Users/tirandagan/dev/stevens/caruca/caruca/outputs/pwd.parity.json`
* v2 traces: `eval/runs/2026-09-14T170947Z_pwd_e9c890ab/pwd.traces.json`

### v1 saw, v2 did not

```text
rf .
```

### v2 reported, v1 did not

_none_


---

## Stage 4 — annotation

* v1: `/Users/tirandagan/dev/stevens/caruca/caruca/outputs/pwd.parity.pash.json`
* v2: `eval/runs/2026-09-14T155618Z_pwd_fdd40d87/pwd.pash.annotation`
* hand-curated ground truth

### v1's annotator

5 case(s)

```json
[
 {
  "predicate": {
   "operator": "and",
   "operands": [
    {
     "operator": "exists",
     "operands": []
    },
    {
     "operator": "len_args_eq",
     "operands": [
      0
     ]
    }
   ]
  },
  "pclass": "side-effectful",
  "inputs": [],
  "outputs": [
   "stdout"
  ],
  "true_str": "pwd",
  "comments": "Automatically generated by caruca."
 },
 {
  "predicate": {
   "operator": "and",
   "operands": [
    {
     "operator": "exists",
     "operands": [
      "-P"
     ]
    },
    {
     "operator": "len_args_eq",
     "operands": [
      0
     ]
    }
   ]
  },
  "pclass": "side-effectful",
  "inputs": [],
… 85 more
```

### v2

1 case(s)

```json
[
 {
  "predicate": "default",
  "pclass": "stateless",
  "inputs": [],
  "outputs": [
   "stdout"
  ],
  "true_str": "pwd",
  "comments": "The `pwd` command reads the current working directory and writes it to stdout. It does not take any input files and does not modify any files."
 }
]
```

### hand-curated ground truth

1 case(s)

```json
[
 {
  "predicate": "default",
  "class": "side-effectful",
  "inputs": [],
  "outputs": [
   "stdout"
  ]
 }
]
```

### v2 against the ground truth

* alignment: `predicate_key` (the two are written at different granularities)
* aligned 1 case(s); class agreeing 0, fully agreeing 0

* `pwd` — **pclass**: v2 `stateless` vs truth `side-effectful`

---

