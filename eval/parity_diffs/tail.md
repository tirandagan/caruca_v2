# Parity diff — `tail`

What v1 produced, what v2 produced, and where they differ. Generated by `scripts/parity_diff.py`; the aggregates are in `ai_docs/analysis/v1_v2_parity_study.md`.

v1 commit `d8032407346aadc135b14c043618c8c1d4f4e0cf` · model `openai/gpt-4o` · temperature 0.0 · bound `--max-arity 1 --max-count 1`

> Quotes v1 source verbatim. v1 is private and unlicensed — read locally, do not commit or share.

---

## Stage 1 — syntax specification
* ground truth: `/Users/tirandagan/dev/stevens/caruca/caruca/src/caruca/syntax_specs/tail.py`
* v1's own LLM output: `/Users/tirandagan/dev/stevens/caruca/outputs/llm-dsl-generation/tail.py`
* v2's output: `eval/runs/2026-09-14T130837Z_tail_833d4436/tail.py`

### v1 LLM output — scored against ground truth

* matched 13 of 13 reference flags; missing 0, spurious 0
* fully agreeing arguments: 11 of 13 (F1 1.000, exact 0.846)

### v2 output — scored against ground truth

* matched 13 of 13 reference flags; missing 0, spurious 0
* fully agreeing arguments: 10 of 13 (F1 1.000, exact 0.769)

### v1's LLM output vs v2's, line by line

```diff
--- v1
+++ v2
@@ -6,3 +6,7 @@
             Integer(flag="-c", alias=["--bytes"]),
-            Selection(flag="-f", alias=["--follow"], choices=("name", "descriptor")),
+            Selection(
+                flag="--follow",
+                choices=("name", "descriptor"),
+                flag_followed_by_equals=True,
+            ),
             Flag("-F"),
@@ -13,3 +17,3 @@
             Flag("--retry"),
-            Integer(flag="-s", alias=["--sleep-interval"]),
+            Duration(flag="-s", alias=["--sleep-interval"]),
             Flag("-v", alias=["--verbose"]),
@@ -22,2 +26 @@
 ]
-
```
---

## Stage 2 — configuration generation

* v1 emitted **41** lines (**28** distinct invocations)
* v2 emitted **77** lines (**77** distinct)
* matched **27**, missing **1**, spurious **50** (recall 0.9642857142857143, precision 0.35064935064935066)
* equivalence: options compared as a multiset of (flag, value) — order-insensitive; operands compared as an ordered sequence; '=' joining, attached short values, and short bundles normalized; shell quoting removed by shlex

### What v1 emitted

```text
tail
tail --help
tail --max-unchanged-stats=-1
tail --max-unchanged-stats=0
tail --max-unchanged-stats=1
tail --pid=-1
tail --pid=0
tail --pid=1
tail --retry
tail --version
tail -F
tail -c=+10
tail -c=0
tail -c=10
tail -f=
tail -f=descriptor
tail -f=name
tail -n=+10
tail -n=0
tail -n=10
tail -q
tail -s=-1
tail -s=0
tail -s=1
tail -v
tail -z
tail abspath_1
tail relpath_1
```

### What v2 emitted

```text
tail --help
tail --help abspath_1
tail --help relpath_1
tail --max-unchanged-stats=-1
tail --max-unchanged-stats=-1 abspath_1
tail --max-unchanged-stats=-1 relpath_1
tail --max-unchanged-stats=0
tail --max-unchanged-stats=0 abspath_1
tail --max-unchanged-stats=0 relpath_1
tail --max-unchanged-stats=1
tail --max-unchanged-stats=1 abspath_1
tail --max-unchanged-stats=1 relpath_1
tail --pid=-1
tail --pid=-1 abspath_1
tail --pid=-1 relpath_1
tail --pid=0
tail --pid=0 abspath_1
tail --pid=0 relpath_1
tail --pid=1
tail --pid=1 abspath_1
tail --pid=1 relpath_1
tail --retry
tail --retry abspath_1
tail --retry relpath_1
tail --version
tail --version abspath_1
tail --version relpath_1
tail -F
tail -F abspath_1
tail -F relpath_1
tail -c=+10
tail -c=+10 abspath_1
tail -c=+10 relpath_1
tail -c=0
tail -c=0 abspath_1
tail -c=0 relpath_1
tail -c=10
tail -c=10 abspath_1
tail -c=10 relpath_1
tail -f=
… 37 more
```

### In v1 but not v2 (missing) — 1 shown

```text
tail
```

### In v2 but not v1 (spurious) — 20 shown

```text
tail --help abspath_1
tail --help relpath_1
tail --max-unchanged-stats=-1 abspath_1
tail --max-unchanged-stats=-1 relpath_1
tail --max-unchanged-stats=0 abspath_1
tail --max-unchanged-stats=0 relpath_1
tail --max-unchanged-stats=1 abspath_1
tail --max-unchanged-stats=1 relpath_1
tail --pid=-1 abspath_1
tail --pid=-1 relpath_1
tail --pid=0 abspath_1
tail --pid=0 relpath_1
tail --pid=1 abspath_1
tail --pid=1 relpath_1
tail --retry abspath_1
tail --retry relpath_1
tail --version abspath_1
tail --version relpath_1
tail -F abspath_1
tail -F relpath_1
```

### Form differences among matches

Of 27 matched invocations, 27 were spelled identically. 0 differed in `=` style and 0 in token order only — reported, never scored.

---

## Stage 3 — execution and tracing

* 1 of 3 cells produced a usable observation
* in the first cell, 0 of 5 sessions called `report_observations`; the rest ended in prose after 2 turns

* v1 recorded 10 raw `(action, path)` pairs, reduced to **2 distinct** by v1's own relevance filter (7 system paths dropped)
* core recall 0.0, inference recall 0.0

* v1 traces: `/Users/tirandagan/dev/stevens/caruca/caruca/outputs/tail.parity.json`
* v2 traces: `eval/runs/2026-09-14T194741Z_tail_20784d24/tail.traces.json`

### v1 saw, v2 did not

```text
rf stdin
wf stdout
```

### v2 reported, v1 did not

_none_


---

## Stage 4 — annotation

* v1: `/Users/tirandagan/dev/stevens/caruca/caruca/outputs/tail.parity.pash.json`
* v2: `eval/runs/2026-09-14T155822Z_tail_8c65f356/tail.pash.annotation`
* hand-curated ground truth

### v1's annotator

9 case(s)

```json
[
 {
  "predicate": {
   "operator": "and",
   "operands": [
    {
     "operator": "exists",
     "operands": []
    },
    {
     "operator": "len_args_eq",
     "operands": [
      0
     ]
    }
   ]
  },
  "pclass": "non-pure",
  "inputs": [
   "stdin"
  ],
  "outputs": [
   "stdout"
  ],
  "true_str": "tail",
  "comments": "Automatically generated by caruca."
 },
 {
  "predicate": {
   "operator": "and",
   "operands": [
    {
     "operator": "exists",
     "operands": [
      "--max-unchanged-stats"
     ]
    },
    {
     "operator": "len_args_eq",
     "operands": [
      0
     ]
    }
   ]
  },
… 201 more
```

### v2

14 case(s)

```json
[
 {
  "predicate": {
   "operator": "len_args_eq",
   "operands": [
    0
   ]
  },
  "pclass": "pure",
  "inputs": [
   "stdin"
  ],
  "outputs": [
   "stdout"
  ],
  "true_str": "tail",
  "comments": "Handles input from stdin and outputs to stdout."
 },
 {
  "predicate": {
   "operator": "exists",
   "operands": [
    "-f",
    "--follow"
   ]
  },
  "pclass": "non-pure",
  "inputs": [
   "stdin"
  ],
  "outputs": [
   "stdout"
  ],
  "true_str": "tail -f",
  "comments": "Handles input from stdin and outputs to stdout with follow option."
 },
 {
  "predicate": {
   "operator": "exists",
   "operands": [
    "--version"
   ]
  },
  "pclass": "pure",
  "inputs": [],
… 200 more
```

### hand-curated ground truth

1 case(s)

```json
[
 {
  "predicate": "default",
  "class": "pure",
  "inputs": [
   "stdin"
  ],
  "outputs": [
   "stdout"
  ]
 }
]
```

### v2 against the ground truth

* alignment: `subsumption` (the two are written at different granularities)
* aligned 14 case(s); class agreeing 8, fully agreeing 5

* `tail -f` — **pclass**: v2 `non-pure` vs truth `pure`
* `tail --version` — **inputs**: v2 `[]` vs truth `['stdin']`
* `tail --help` — **inputs**: v2 `[]` vs truth `['stdin']`
* `tail --retry` — **pclass**: v2 `non-pure` vs truth `pure`
* `tail --max-unchanged-stats` — **pclass**: v2 `non-pure` vs truth `pure`
* `tail --pid` — **pclass**: v2 `non-pure` vs truth `pure`
* `tail -s` — **pclass**: v2 `non-pure` vs truth `pure`
* `tail -F` — **pclass**: v2 `non-pure` vs truth `pure`

---

