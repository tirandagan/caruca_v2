# Parity diff — `cat`

What v1 produced, what v2 produced, and where they differ. Generated by `scripts/parity_diff.py`; the aggregates are in `ai_docs/analysis/v1_v2_parity_study.md`.

v1 commit `d8032407346aadc135b14c043618c8c1d4f4e0cf` · model `openai/gpt-4o` · temperature 0.0 · bound `--max-arity 1 --max-count 1`

> Quotes v1 source verbatim. v1 is private and unlicensed — read locally, do not commit or share.

---

## Stage 1 — syntax specification
* ground truth: `/Users/tirandagan/dev/stevens/caruca/caruca/src/caruca/syntax_specs/cat.py`
* v1's own LLM output: `/Users/tirandagan/dev/stevens/caruca/outputs/llm-dsl-generation/cat.py`
* v2's output: `eval/runs/2026-09-14T130803Z_cat_75b37300/cat.py`

### v1 LLM output — scored against ground truth

* matched 12 of 12 reference flags; missing 0, spurious 0
* fully agreeing arguments: 12 of 12 (F1 1.000, exact 1.000)

### v2 output — scored against ground truth

* matched 12 of 12 reference flags; missing 0, spurious 0
* fully agreeing arguments: 12 of 12 (F1 1.000, exact 1.000)

### v1's LLM output vs v2's, line by line

```diff
--- v1
+++ v2
@@ -21,2 +21 @@
 ]
-
```
---

## Stage 2 — configuration generation

* v1 emitted **27** lines (**15** distinct invocations)
* v2 emitted **39** lines (**39** distinct)
* matched **15**, missing **0**, spurious **24** (recall 1.0, precision 0.38461538461538464)
* equivalence: options compared as a multiset of (flag, value) — order-insensitive; operands compared as an ordered sequence; '=' joining, attached short values, and short bundles normalized; shell quoting removed by shlex

### What v1 emitted

```text
cat
cat --help
cat --version
cat -A
cat -E
cat -T
cat -b
cat -e
cat -n
cat -s
cat -t
cat -u
cat -v
cat abspath_1
cat relpath_1
```

### What v2 emitted

```text
cat
cat --help
cat --help abspath_1
cat --help relpath_1
cat --version
cat --version abspath_1
cat --version relpath_1
cat -A
cat -A abspath_1
cat -A relpath_1
cat -E
cat -E abspath_1
cat -E relpath_1
cat -T
cat -T abspath_1
cat -T relpath_1
cat -b
cat -b abspath_1
cat -b relpath_1
cat -e
cat -e abspath_1
cat -e relpath_1
cat -n
cat -n abspath_1
cat -n relpath_1
cat -s
cat -s abspath_1
cat -s relpath_1
cat -t
cat -t abspath_1
cat -t relpath_1
cat -u
cat -u abspath_1
cat -u relpath_1
cat -v
cat -v abspath_1
cat -v relpath_1
cat abspath_1
cat relpath_1
```

### In v1 but not v2 (missing) — 0 shown

_none_


### In v2 but not v1 (spurious) — 20 shown

```text
cat --help abspath_1
cat --help relpath_1
cat --version abspath_1
cat --version relpath_1
cat -A abspath_1
cat -A relpath_1
cat -E abspath_1
cat -E relpath_1
cat -T abspath_1
cat -T relpath_1
cat -b abspath_1
cat -b relpath_1
cat -e abspath_1
cat -e relpath_1
cat -n abspath_1
cat -n relpath_1
cat -s abspath_1
cat -s relpath_1
cat -t abspath_1
cat -t relpath_1
```

### Form differences among matches

Of 15 matched invocations, 15 were spelled identically. 0 differed in `=` style and 0 in token order only — reported, never scored.

---

## Stage 3 — execution and tracing

* 1 of 3 cells produced a usable observation
* in the first cell, 4 of 5 sessions called `report_observations`; the rest ended in prose after 2 turns

* v1 recorded 40 raw `(action, path)` pairs, reduced to **2 distinct** by v1's own relevance filter (28 system paths dropped)
* core recall 1.0, inference recall 1.0

* v1 traces: `/Users/tirandagan/dev/stevens/caruca/caruca/outputs/cat.parity.json`
* v2 traces: `eval/runs/2026-09-14T163935Z_cat_a3ea2a72/cat.traces.json`

### v1 saw, v2 did not

_none_


### v2 reported, v1 did not

_none_


---

## Stage 4 — annotation

* v1: `/Users/tirandagan/dev/stevens/caruca/caruca/outputs/cat.parity.pash.json`
* v2: `eval/runs/2026-09-14T155525Z_cat_8ed69ec8/cat.pash.annotation`
* hand-curated ground truth

### v1's annotator

14 case(s)

```json
[
 {
  "predicate": {
   "operator": "and",
   "operands": [
    {
     "operator": "exists",
     "operands": []
    },
    {
     "operator": "len_args_eq",
     "operands": [
      0
     ]
    }
   ]
  },
  "pclass": "non-pure",
  "inputs": [
   "stdin"
  ],
  "outputs": [
   "stdout"
  ],
  "true_str": "cat",
  "comments": "Automatically generated by caruca."
 },
 {
  "predicate": {
   "operator": "and",
   "operands": [
    {
     "operator": "exists",
     "operands": [
      "-b"
     ]
    },
    {
     "operator": "len_args_eq",
     "operands": [
      0
     ]
    }
   ]
  },
… 341 more
```

### v2

2 case(s)

```json
[
 {
  "predicate": "default",
  "pclass": "pure",
  "inputs": [
   "stdin"
  ],
  "outputs": [
   "stdout"
  ],
  "true_str": "cat",
  "comments": "The command reads from stdin and writes to stdout. It is pure as it does not modify any files or directories."
 },
 {
  "predicate": {
   "operator": "exists",
   "operands": [
    "PATH_HECYP"
   ]
  },
  "pclass": "pure",
  "inputs": [
   "PATH_HECYP"
  ],
  "outputs": [
   "stdout"
  ],
  "true_str": "cat PATH_HECYP",
  "comments": "The command reads from the specified file paths and writes to stdout. It is pure as it does not modify any files or directories."
 }
]
```

### hand-curated ground truth

3 case(s)

```json
[
 {
  "predicate": {
   "operator": "exists",
   "operands": [
    "-n"
   ]
  },
  "class": "pure",
  "inputs": [
   "args[:]"
  ],
  "outputs": [
   "stdout"
  ],
  "comment": "Pure because -n prints line numbering, and splitting affects it"
 },
 {
  "predicate": {
   "operator": "len_args_eq",
   "operands": [
    0
   ]
  },
  "comment": ":')",
  "class": "stateless",
  "inputs": [
   "stdin"
  ],
  "outputs": [
   "stdout"
  ]
 },
 {
  "predicate": "default",
  "class": "stateless",
  "inputs": [
   "args[:]"
  ],
  "outputs": [
   "stdout"
  ],
  "options": [
   "stdin-hyphen"
  ]
… 2 more
```

### v2 against the ground truth

* alignment: `predicate_key` (the two are written at different granularities)
* aligned 1 case(s); class agreeing 0, fully agreeing 0

* `cat` — **pclass**: v2 `pure` vs truth `stateless`
* `cat` — **inputs**: v2 `['stdin']` vs truth `['args[:]']`

---

