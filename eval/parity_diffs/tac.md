# Parity diff — `tac`

What v1 produced, what v2 produced, and where they differ. Generated by `scripts/parity_diff.py`; the aggregates are in `ai_docs/analysis/v1_v2_parity_study.md`.

v1 commit `d8032407346aadc135b14c043618c8c1d4f4e0cf` · model `openai/gpt-4o` · temperature 0.0 · bound `--max-arity 1 --max-count 1`

> Quotes v1 source verbatim. v1 is private and unlicensed — read locally, do not commit or share.

---

## Stage 1 — syntax specification
* ground truth: `/Users/tirandagan/dev/stevens/caruca/caruca/src/caruca/syntax_specs/tac.py`
* v1's own LLM output: `/Users/tirandagan/dev/stevens/caruca/outputs/llm-dsl-generation/tac.py`
* v2's output: `eval/runs/2026-09-14T130832Z_tac_cf9be720/tac.py`

### v1 LLM output — scored against ground truth

* matched 5 of 5 reference flags; missing 0, spurious 0
* fully agreeing arguments: 5 of 5 (F1 1.000, exact 1.000)

### v2 output — scored against ground truth

* matched 5 of 5 reference flags; missing 0, spurious 0
* fully agreeing arguments: 4 of 5 (F1 1.000, exact 0.800)

### v1's LLM output vs v2's, line by line

```diff
--- v1
+++ v2
@@ -7,3 +7,3 @@
             Flag("-r", alias=["--regex"]),
-            Separator(flag="-s", alias=["--separator"]),
+            String(flag="-s", alias=["--separator"]),
             Flag("--help"),
@@ -14,2 +14 @@
 ]
-
```
---

## Stage 2 — configuration generation

* v1 emitted **15** lines (**8** distinct invocations)
* v2 emitted **21** lines (**21** distinct)
* matched **8**, missing **0**, spurious **13** (recall 1.0, precision 0.38095238095238093)
* equivalence: options compared as a multiset of (flag, value) — order-insensitive; operands compared as an ordered sequence; '=' joining, attached short values, and short bundles normalized; shell quoting removed by shlex

### What v1 emitted

```text
	'
tac
tac --help
tac --version
tac -b
tac -r
tac -s '
tac -s ,
tac abspath_1
tac relpath_1
```

### What v2 emitted

```text
	
	 abspath_1
	 relpath_1
tac
tac --help
tac --help abspath_1
tac --help relpath_1
tac --version
tac --version abspath_1
tac --version relpath_1
tac -b
tac -b abspath_1
tac -b relpath_1
tac -r
tac -r abspath_1
tac -r relpath_1
tac -s 
tac -s ,
tac -s , abspath_1
tac -s , relpath_1
tac abspath_1
tac relpath_1
```

### In v1 but not v2 (missing) — 0 shown

_none_


### In v2 but not v1 (spurious) — 13 shown

```text
tac --help abspath_1
tac --help relpath_1
tac --version abspath_1
tac --version relpath_1
tac -b abspath_1
tac -b relpath_1
tac -r abspath_1
tac -r relpath_1
tac -s 
	
tac -s 
	 abspath_1
tac -s 
	 relpath_1
tac -s , abspath_1
tac -s , relpath_1
```

### Form differences among matches

Of 8 matched invocations, 8 were spelled identically. 0 differed in `=` style and 0 in token order only — reported, never scored.

---

## Stage 3 — execution and tracing

* 0 of 3 cells produced a usable observation
* in the first cell, 0 of 5 sessions called `report_observations`; the rest ended in prose after 2 turns

_no v2 traces to compare_

---

## Stage 4 — annotation

* v1: `/Users/tirandagan/dev/stevens/caruca/caruca/outputs/tac.parity.pash.json`
* v2: `eval/runs/2026-09-14T155737Z_tac_960931de/tac.pash.annotation`
* hand-curated ground truth

### v1's annotator

7 case(s)

```json
[
 {
  "predicate": {
   "operator": "and",
   "operands": [
    {
     "operator": "exists",
     "operands": []
    },
    {
     "operator": "len_args_eq",
     "operands": [
      0
     ]
    }
   ]
  },
  "pclass": "side-effectful",
  "inputs": [
   "stdin"
  ],
  "outputs": [
   "/tmp/tmpt01yk756try_garbage/cutmpHDTsjX",
   "/tmp/tmpt01yk756try_garbage/cutmpMrh7lv",
   "/tmp/tmpt01yk756try_garbage/cutmpTUnJ7I",
   "/tmp/tmpt01yk756try_garbage/cutmpciTVvA",
   "/tmp/tmpt01yk756try_garbage/cutmpgB8BDi",
   "/tmp/tmpt01yk756try_garbage/cutmpl8kWA0",
   "stdout"
  ],
  "true_str": "tac",
  "comments": "Automatically generated by caruca."
 },
 {
  "predicate": {
   "operator": "and",
   "operands": [
    {
     "operator": "exists",
     "operands": [
      "-r"
     ]
    },
    {
     "operator": "len_args_eq",
… 155 more
```

### v2

2 case(s)

```json
[
 {
  "predicate": "default",
  "pclass": "pure",
  "inputs": [
   "stdin"
  ],
  "outputs": [
   "stdout"
  ],
  "true_str": "tac",
  "comments": "The command reads from stdin and writes to stdout, reversing the order of lines."
 },
 {
  "predicate": {
   "operator": "exists",
   "operands": [
    "PATH_ABHEX"
   ]
  },
  "pclass": "pure",
  "inputs": [
   "PATH_ABHEX"
  ],
  "outputs": [
   "stdout"
  ],
  "true_str": "tac relpath_1",
  "comments": "The command reads from a file specified by a path and writes to stdout, reversing the order of lines."
 }
]
```

### hand-curated ground truth

1 case(s)

```json
[
 {
  "predicate": "default",
  "class": "stateless",
  "inputs": [
   "args[:]"
  ],
  "outputs": [
   "stdout"
  ]
 }
]
```

### v2 against the ground truth

* alignment: `predicate_key` (the two are written at different granularities)
* aligned 1 case(s); class agreeing 0, fully agreeing 0

* `tac` — **pclass**: v2 `pure` vs truth `stateless`
* `tac` — **inputs**: v2 `['stdin']` vs truth `['args[:]']`

---

