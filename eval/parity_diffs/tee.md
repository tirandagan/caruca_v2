# Parity diff — `tee`

What v1 produced, what v2 produced, and where they differ. Generated by `scripts/parity_diff.py`; the aggregates are in `ai_docs/analysis/v1_v2_parity_study.md`.

v1 commit `d8032407346aadc135b14c043618c8c1d4f4e0cf` · model `openai/gpt-4o` · temperature 0.0 · bound `--max-arity 1 --max-count 1`

> Quotes v1 source verbatim. v1 is private and unlicensed — read locally, do not commit or share.

---

## Stage 1 — syntax specification
* ground truth: `/Users/tirandagan/dev/stevens/caruca/caruca/src/caruca/syntax_specs/tee.py`
* v1's own LLM output: `/Users/tirandagan/dev/stevens/caruca/outputs/llm-dsl-generation/tee.py`
* v2's output: `eval/runs/2026-09-14T130847Z_tee_35d5adde/tee.py`

### v1 LLM output — scored against ground truth

* matched 6 of 6 reference flags; missing 0, spurious 0
* fully agreeing arguments: 6 of 6 (F1 1.000, exact 1.000)

### v2 output — scored against ground truth

* matched 6 of 6 reference flags; missing 0, spurious 0
* fully agreeing arguments: 6 of 6 (F1 1.000, exact 1.000)

### v1's LLM output vs v2's, line by line

```diff
--- v1
+++ v2
@@ -19,2 +19 @@
 ]
-
```
---

## Stage 2 — configuration generation

* v1 emitted **30** lines (**20** distinct invocations)
* v2 emitted **18** lines (**18** distinct)
* matched **18**, missing **2**, spurious **0** (recall 0.9, precision 1.0)
* equivalence: options compared as a multiset of (flag, value) — order-insensitive; operands compared as an ordered sequence; '=' joining, attached short values, and short bundles normalized; shell quoting removed by shlex

### What v1 emitted

```text
tee --help abspath_1
tee --help relpath_1
tee --output-error=exit abspath_1
tee --output-error=exit relpath_1
tee --output-error=exit-nopipe abspath_1
tee --output-error=exit-nopipe relpath_1
tee --output-error=warn abspath_1
tee --output-error=warn relpath_1
tee --output-error=warn-nopipe abspath_1
tee --output-error=warn-nopipe relpath_1
tee --version abspath_1
tee --version relpath_1
tee -a abspath_1
tee -a relpath_1
tee -i abspath_1
tee -i relpath_1
tee -p abspath_1
tee -p relpath_1
tee abspath_1
tee relpath_1
```

### What v2 emitted

```text
tee --help abspath_1
tee --help relpath_1
tee --output-error=exit abspath_1
tee --output-error=exit relpath_1
tee --output-error=exit-nopipe abspath_1
tee --output-error=exit-nopipe relpath_1
tee --output-error=warn abspath_1
tee --output-error=warn relpath_1
tee --output-error=warn-nopipe abspath_1
tee --output-error=warn-nopipe relpath_1
tee --version abspath_1
tee --version relpath_1
tee -a abspath_1
tee -a relpath_1
tee -i abspath_1
tee -i relpath_1
tee -p abspath_1
tee -p relpath_1
```

### In v1 but not v2 (missing) — 2 shown

```text
tee abspath_1
tee relpath_1
```

### In v2 but not v1 (spurious) — 0 shown

_none_


### Form differences among matches

Of 18 matched invocations, 18 were spelled identically. 0 differed in `=` style and 0 in token order only — reported, never scored.

---

## Stage 3 — execution and tracing

* 1 of 3 cells produced a usable observation
* in the first cell, 0 of 5 sessions called `report_observations`; the rest ended in prose after 4 turns

* v1 recorded 14 raw `(action, path)` pairs, reduced to **5 distinct** by v1's own relevance filter (7 system paths dropped)
* core recall 0.25, inference recall 1.0

* v1 traces: `/Users/tirandagan/dev/stevens/caruca/caruca/outputs/tee.parity.json`
* v2 traces: `eval/runs/2026-09-14T223407Z_tee_6ba13e86/tee.traces.json`

### v1 saw, v2 did not

```text
mo /tmp/sandbox_outer/sandbox_inner/relpath_1
wf /tmp/sandbox_outer/sandbox_inner/relpath_1
wf relpath_1
```

### v2 reported, v1 did not

```text
mo relpath_1
```

---

## Stage 4 — annotation

v2 produced no annotation. Reason:

```text
BadRequestError: Error code: 400 - {'error': {'message': "This endpoint's maximum context length is 128000 tokens. However, you requested about 158982 tokens (154886 of text input, 4096 in the output). Please reduce the length of either one, or use the context-compression plugin to compress your prompt automatically.", 'code': 400, 'metadata': {'provider_name': None}}}
```

---

