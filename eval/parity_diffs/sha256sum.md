# Parity diff — `sha256sum`

What v1 produced, what v2 produced, and where they differ. Generated by `scripts/parity_diff.py`; the aggregates are in `ai_docs/analysis/v1_v2_parity_study.md`.

v1 commit `d8032407346aadc135b14c043618c8c1d4f4e0cf` · model `openai/gpt-4o` · temperature 0.0 · bound `--max-arity 1 --max-count 1`

> Quotes v1 source verbatim. v1 is private and unlicensed — read locally, do not commit or share.

---

## Stage 1 — syntax specification
* ground truth: `/Users/tirandagan/dev/stevens/caruca/caruca/src/caruca/syntax_specs/sha256sum.py`
* v1's own LLM output: `/Users/tirandagan/dev/stevens/caruca/outputs/llm-dsl-generation/sha256sum.py`
* v2's output: `eval/runs/2026-09-14T130825Z_sha256sum_dbe6d9b9/sha256sum.py`

### v1 LLM output — scored against ground truth

* matched 12 of 12 reference flags; missing 0, spurious 0
* fully agreeing arguments: 12 of 12 (F1 1.000, exact 1.000)

### v2 output — scored against ground truth

* matched 12 of 12 reference flags; missing 0, spurious 0
* fully agreeing arguments: 12 of 12 (F1 1.000, exact 1.000)

### v1's LLM output vs v2's, line by line

```diff
--- v1
+++ v2
@@ -21,2 +21 @@
 ]
-
```
---

## Stage 2 — configuration generation

* v1 emitted **27** lines (**15** distinct invocations)
* v2 emitted **39** lines (**39** distinct)
* matched **15**, missing **0**, spurious **24** (recall 1.0, precision 0.38461538461538464)
* equivalence: options compared as a multiset of (flag, value) — order-insensitive; operands compared as an ordered sequence; '=' joining, attached short values, and short bundles normalized; shell quoting removed by shlex

### What v1 emitted

```text
sha256sum
sha256sum --help
sha256sum --ignore-missing
sha256sum --quiet
sha256sum --status
sha256sum --strict
sha256sum --tag
sha256sum --version
sha256sum -b
sha256sum -c
sha256sum -t
sha256sum -w
sha256sum -z
sha256sum abspath_1
sha256sum relpath_1
```

### What v2 emitted

```text
sha256sum
sha256sum --help
sha256sum --help abspath_1
sha256sum --help relpath_1
sha256sum --ignore-missing
sha256sum --ignore-missing abspath_1
sha256sum --ignore-missing relpath_1
sha256sum --quiet
sha256sum --quiet abspath_1
sha256sum --quiet relpath_1
sha256sum --status
sha256sum --status abspath_1
sha256sum --status relpath_1
sha256sum --strict
sha256sum --strict abspath_1
sha256sum --strict relpath_1
sha256sum --tag
sha256sum --tag abspath_1
sha256sum --tag relpath_1
sha256sum --version
sha256sum --version abspath_1
sha256sum --version relpath_1
sha256sum -b
sha256sum -b abspath_1
sha256sum -b relpath_1
sha256sum -c
sha256sum -c abspath_1
sha256sum -c relpath_1
sha256sum -t
sha256sum -t abspath_1
sha256sum -t relpath_1
sha256sum -w
sha256sum -w abspath_1
sha256sum -w relpath_1
sha256sum -z
sha256sum -z abspath_1
sha256sum -z relpath_1
sha256sum abspath_1
sha256sum relpath_1
```

### In v1 but not v2 (missing) — 0 shown

_none_


### In v2 but not v1 (spurious) — 20 shown

```text
sha256sum --help abspath_1
sha256sum --help relpath_1
sha256sum --ignore-missing abspath_1
sha256sum --ignore-missing relpath_1
sha256sum --quiet abspath_1
sha256sum --quiet relpath_1
sha256sum --status abspath_1
sha256sum --status relpath_1
sha256sum --strict abspath_1
sha256sum --strict relpath_1
sha256sum --tag abspath_1
sha256sum --tag relpath_1
sha256sum --version abspath_1
sha256sum --version relpath_1
sha256sum -b abspath_1
sha256sum -b relpath_1
sha256sum -c abspath_1
sha256sum -c relpath_1
sha256sum -t abspath_1
sha256sum -t relpath_1
```

### Form differences among matches

Of 15 matched invocations, 15 were spelled identically. 0 differed in `=` style and 0 in token order only — reported, never scored.

---

## Stage 3 — execution and tracing

* 3 of 3 cells produced a usable observation
* in the first cell, 1 of 5 sessions called `report_observations`; the rest ended in prose after 2 turns

* v1 recorded 13 raw `(action, path)` pairs, reduced to **2 distinct** by v1's own relevance filter (10 system paths dropped)
* core recall 1.0, inference recall 1.0

* v1 traces: `/Users/tirandagan/dev/stevens/caruca/caruca/outputs/sha256sum.parity.json`
* v2 traces: `eval/runs/2026-09-14T173047Z_sha256sum_0c2e56f8/sha256sum.traces.json`

### v1 saw, v2 did not

_none_


### v2 reported, v1 did not

_none_


---

## Stage 4 — annotation

* v1: `/Users/tirandagan/dev/stevens/caruca/caruca/outputs/sha256sum.parity.pash.json`
* v2: `eval/runs/2026-09-14T155638Z_sha256sum_5e28d814/sha256sum.pash.annotation`
* hand-curated ground truth

### v1's annotator

8 case(s)

```json
[
 {
  "predicate": {
   "operator": "and",
   "operands": [
    {
     "operator": "exists",
     "operands": []
    },
    {
     "operator": "len_args_eq",
     "operands": [
      0
     ]
    }
   ]
  },
  "pclass": "non-pure",
  "inputs": [
   "stdin"
  ],
  "outputs": [
   "stdout"
  ],
  "true_str": "sha256sum",
  "comments": "Automatically generated by caruca."
 },
 {
  "predicate": {
   "operator": "and",
   "operands": [
    {
     "operator": "exists",
     "operands": [
      "-z"
     ]
    },
    {
     "operator": "len_args_eq",
     "operands": [
      0
     ]
    }
   ]
  },
… 173 more
```

### v2

14 case(s)

```json
[
 {
  "predicate": {
   "operator": "len_args_eq",
   "operands": [
    0
   ]
  },
  "pclass": "pure",
  "inputs": [
   "stdin"
  ],
  "outputs": [
   "stdout"
  ],
  "true_str": "sha256sum",
  "comments": "Reads from stdin and writes the SHA256 checksum to stdout."
 },
 {
  "predicate": {
   "operator": "exists",
   "operands": [
    "-c"
   ]
  },
  "pclass": "pure",
  "inputs": [
   "stdin"
  ],
  "outputs": [
   "stderr"
  ],
  "true_str": "sha256sum -c",
  "comments": "Verifies checksums from stdin, outputs errors to stderr."
 },
 {
  "predicate": {
   "operator": "exists",
   "operands": [
    "--help"
   ]
  },
  "pclass": "pure",
  "inputs": [],
  "outputs": [
… 191 more
```

### hand-curated ground truth

1 case(s)

```json
[
 {
  "predicate": "default",
  "class": "non-pure",
  "inputs": [
   "stdin"
  ],
  "outputs": [
   "stdout"
  ]
 }
]
```

### v2 against the ground truth

* alignment: `subsumption` (the two are written at different granularities)
* aligned 14 case(s); class agreeing 0, fully agreeing 0

* `sha256sum` — **pclass**: v2 `pure` vs truth `non-pure`
* `sha256sum -c` — **pclass**: v2 `pure` vs truth `non-pure`
* `sha256sum -c` — **outputs**: v2 `['stderr']` vs truth `['stdout']`
* `sha256sum --help` — **pclass**: v2 `pure` vs truth `non-pure`
* `sha256sum --help` — **inputs**: v2 `[]` vs truth `['stdin']`
* `sha256sum --version` — **pclass**: v2 `pure` vs truth `non-pure`
* `sha256sum --version` — **inputs**: v2 `[]` vs truth `['stdin']`
* `sha256sum -z` — **pclass**: v2 `pure` vs truth `non-pure`

---

