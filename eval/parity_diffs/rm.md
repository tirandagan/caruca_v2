# Parity diff — `rm`

What v1 produced, what v2 produced, and where they differ. Generated by `scripts/parity_diff.py`; the aggregates are in `ai_docs/analysis/v1_v2_parity_study.md`.

v1 commit `d8032407346aadc135b14c043618c8c1d4f4e0cf` · model `openai/gpt-4o` · temperature 0.0 · bound `--max-arity 1 --max-count 1`

> Quotes v1 source verbatim. v1 is private and unlicensed — read locally, do not commit or share.

---

## Stage 1 — syntax specification
* ground truth: `/Users/tirandagan/dev/stevens/caruca/caruca/src/caruca/syntax_specs/rm.py`
* v1's own LLM output: `/Users/tirandagan/dev/stevens/caruca/outputs/llm-dsl-generation/rm.py`
* v2's output: `eval/runs/2026-09-14T130816Z_rm_e8d48780/rm.py`

### v1 LLM output — scored against ground truth

* matched 12 of 12 reference flags; missing 0, spurious 0
* fully agreeing arguments: 12 of 12 (F1 1.000, exact 1.000)

### v2 output — scored against ground truth

* matched 12 of 12 reference flags; missing 0, spurious 0
* fully agreeing arguments: 12 of 12 (F1 1.000, exact 1.000)

### v1's LLM output vs v2's, line by line

```diff
--- v1
+++ v2
@@ -23,3 +23,3 @@
                 flag="--preserve-root",
-                choices=("", "all"),
+                choices=(["", "all"]),
                 flag_followed_by_equals=True,
@@ -35,2 +35 @@
 ]
-
```
---

## Stage 2 — configuration generation

* v1 emitted **54** lines (**32** distinct invocations)
* v2 emitted **32** lines (**32** distinct)
* matched **32**, missing **0**, spurious **0** (recall 1.0, precision 1.0)
* equivalence: options compared as a multiset of (flag, value) — order-insensitive; operands compared as an ordered sequence; '=' joining, attached short values, and short bundles normalized; shell quoting removed by shlex

### What v1 emitted

```text
rm --help abspath_1
rm --help relpath_1
rm --interactive=always abspath_1
rm --interactive=always relpath_1
rm --interactive=never abspath_1
rm --interactive=never relpath_1
rm --interactive=once abspath_1
rm --interactive=once relpath_1
rm --no-preserve-root abspath_1
rm --no-preserve-root relpath_1
rm --one-file-system abspath_1
rm --one-file-system relpath_1
rm --preserve-root= abspath_1
rm --preserve-root= relpath_1
rm --preserve-root=all abspath_1
rm --preserve-root=all relpath_1
rm --version abspath_1
rm --version relpath_1
rm -I abspath_1
rm -I relpath_1
rm -d abspath_1
rm -d relpath_1
rm -f abspath_1
rm -f relpath_1
rm -i abspath_1
rm -i relpath_1
rm -r abspath_1
rm -r relpath_1
rm -v abspath_1
rm -v relpath_1
rm abspath_1
rm relpath_1
```

### What v2 emitted

```text
rm --help abspath_1
rm --help relpath_1
rm --interactive=always abspath_1
rm --interactive=always relpath_1
rm --interactive=never abspath_1
rm --interactive=never relpath_1
rm --interactive=once abspath_1
rm --interactive=once relpath_1
rm --no-preserve-root abspath_1
rm --no-preserve-root relpath_1
rm --one-file-system abspath_1
rm --one-file-system relpath_1
rm --preserve-root= abspath_1
rm --preserve-root= relpath_1
rm --preserve-root=all abspath_1
rm --preserve-root=all relpath_1
rm --version abspath_1
rm --version relpath_1
rm -I abspath_1
rm -I relpath_1
rm -d abspath_1
rm -d relpath_1
rm -f abspath_1
rm -f relpath_1
rm -i abspath_1
rm -i relpath_1
rm -r abspath_1
rm -r relpath_1
rm -v abspath_1
rm -v relpath_1
rm abspath_1
rm relpath_1
```

### In v1 but not v2 (missing) — 0 shown

_none_


### In v2 but not v1 (spurious) — 0 shown

_none_


### Form differences among matches

Of 32 matched invocations, 32 were spelled identically. 0 differed in `=` style and 0 in token order only — reported, never scored.

---

## Stage 3 — execution and tracing

* 3 of 3 cells produced a usable observation
* in the first cell, 3 of 5 sessions called `report_observations`; the rest ended in prose after 3 turns

* v1 recorded 34 raw `(action, path)` pairs, reduced to **2 distinct** by v1's own relevance filter (24 system paths dropped)
* core recall 0.0, inference recall None

* v1 traces: `/Users/tirandagan/dev/stevens/caruca/caruca/outputs/rm.parity.json`
* v2 traces: `eval/runs/2026-09-14T171048Z_rm_2a72de0d/rm.traces.json`

### v1 saw, v2 did not

```text
de /tmp/sandbox_outer/sandbox_inner/relpath_1
wf stderr
```

### v2 reported, v1 did not

```text
de relpath_1
```

---

## Stage 4 — annotation

v2 produced no annotation. Reason:

```text
BadRequestError: Error code: 400 - {'error': {'message': "This endpoint's maximum context length is 128000 tokens. However, you requested about 224394 tokens (220298 of text input, 4096 in the output). Please reduce the length of either one, or use the context-compression plugin to compress your prompt automatically.", 'code': 400, 'metadata': {'provider_name': None}}}
```

---

