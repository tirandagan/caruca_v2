{
  "command": "sha256sum",
  "cases": [
    {
      "predicate": {
        "operator": "len_args_eq",
        "operands": [0]
      },
      "pclass": "pure",
      "inputs": ["stdin"],
      "outputs": ["stdout"],
      "true_str": "sha256sum",
      "comments": "Reads from stdin and writes the SHA256 checksum to stdout."
    },
    {
      "predicate": {
        "operator": "exists",
        "operands": ["-c"]
      },
      "pclass": "non-pure",
      "inputs": ["stdin"],
      "outputs": ["stderr"],
      "true_str": "sha256sum -c",
      "comments": "Verifies checksums from stdin, outputs errors to stderr."
    },
    {
      "predicate": {
        "operator": "exists",
        "operands": ["--help"]
      },
      "pclass": "pure",
      "inputs": [],
      "outputs": ["stdout"],
      "true_str": "sha256sum --help",
      "comments": "Displays help information."
    },
    {
      "predicate": {
        "operator": "exists",
        "operands": ["--version"]
      },
      "pclass": "pure",
      "inputs": [],
      "outputs": ["stdout"],
      "true_str": "sha256sum --version",
      "comments": "Displays version information."
    },
    {
      "predicate": {
        "operator": "exists",
        "operands": ["-b", "-t", "-z", "--tag"]
      },
      "pclass": "pure",
      "inputs": ["stdin"],
      "outputs": ["stdout"],
      "true_str": "sha256sum -b|-t|-z|--tag",
      "comments": "Reads from stdin and writes the SHA256 checksum to stdout with specified formatting."
    },
    {
      "predicate": {
        "operator": "exists",
        "operands": ["--status", "--quiet", "--strict", "--ignore-missing", "-w"]
      },
      "pclass": "non-pure",
      "inputs": ["stdin"],
      "outputs": ["stderr"],
      "true_str": "sha256sum --status|--quiet|--strict|--ignore-missing|-w",
      "comments": "Options meaningful only when verifying checksums, outputs errors to stderr."
    },
    {
      "predicate": {
        "operator": "len_args_eq",
        "operands": [1]
      },
      "pclass": "pure",
      "inputs": ["file"],
      "outputs": ["stdout"],
      "true_str": "sha256sum [FILE]",
      "comments": "Reads from a file and writes the SHA256 checksum to stdout."
    }
  ],
  "options": [
    "-c",
    "--status",
    "--quiet",
    "--strict",
    "--ignore-missing",
    "-w",
    "--help",
    "--version",
    "-b",
    "-t",
    "-z",
    "--tag"
  ]
}
