{
  "command": "cat",
  "cases": [
    {
      "predicate": "default",
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat",
      "comments": "The command reads from stdin and writes to stdout. It is stateless and can be parallelized."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "-b"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat -b",
      "comments": "The command with -b flag numbers non-empty output lines. It reads from stdin and writes to stdout. It is stateless and can be parallelized."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "-n"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat -n",
      "comments": "The command with -n flag numbers all output lines. It reads from stdin and writes to stdout. It is stateless and can be parallelized."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "-T"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat -T",
      "comments": "The command with -T flag displays TAB characters as ^I. It reads from stdin and writes to stdout. It is stateless and can be parallelized."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "-A"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat -A",
      "comments": "The command with -A flag is equivalent to -vET. It reads from stdin and writes to stdout. It is stateless and can be parallelized."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "-E"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat -E",
      "comments": "The command with -E flag displays $ at the end of each line. It reads from stdin and writes to stdout. It is stateless and can be parallelized."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "-v"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat -v",
      "comments": "The command with -v flag uses ^ and M- notation, except for LFD and TAB. It reads from stdin and writes to stdout. It is stateless and can be parallelized."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "-t"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat -t",
      "comments": "The command with -t flag is equivalent to -vT. It reads from stdin and writes to stdout. It is stateless and can be parallelized."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "-e"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat -e",
      "comments": "The command with -e flag is equivalent to -vE. It reads from stdin and writes to stdout. It is stateless and can be parallelized."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "-s"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat -s",
      "comments": "The command with -s flag suppresses repeated empty output lines. It reads from stdin and writes to stdout. It is stateless and can be parallelized."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "-u"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat -u",
      "comments": "The command with -u flag is ignored. It reads from stdin and writes to stdout. It is stateless and can be parallelized."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "--help"
        ]
      },
      "pclass": "stateless",
      "inputs": [],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat --help",
      "comments": "The command with --help flag displays help information. It writes to stdout. It is stateless and can be parallelized."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "--version"
        ]
      },
      "pclass": "stateless",
      "inputs": [],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat --version",
      "comments": "The command with --version flag displays version information. It writes to stdout. It is stateless and can be parallelized."
    }
  ],
  "options": [
    "-b",
    "-n",
    "-T",
    "-A",
    "-E",
    "-v",
    "-t",
    "-e",
    "-s",
    "-u",
    "--help",
    "--version"
  ]
}
