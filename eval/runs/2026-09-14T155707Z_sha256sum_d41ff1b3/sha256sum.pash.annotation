{
  "command": "sha256sum",
  "cases": [
    {
      "predicate": {
        "operator": "len_args_eq",
        "operands": [0]
      },
      "pclass": "pure",
      "inputs": ["stdin"],
      "outputs": ["stdout"],
      "true_str": "sha256sum",
      "comments": "Reads from stdin and writes the SHA256 checksum to stdout."
    },
    {
      "predicate": {
        "operator": "exists",
        "operands": ["-c"]
      },
      "pclass": "pure",
      "inputs": ["stdin"],
      "outputs": ["stderr"],
      "true_str": "sha256sum -c",
      "comments": "Verifies checksums from stdin, outputs errors to stderr."
    },
    {
      "predicate": {
        "operator": "exists",
        "operands": ["--status"]
      },
      "pclass": "pure",
      "inputs": [],
      "outputs": ["stderr"],
      "true_str": "sha256sum --status",
      "comments": "The --status option is meaningful only when verifying checksums."
    },
    {
      "predicate": {
        "operator": "exists",
        "operands": ["-z"]
      },
      "pclass": "pure",
      "inputs": ["stdin"],
      "outputs": ["stdout"],
      "true_str": "sha256sum -z",
      "comments": "Outputs the checksum with a NUL terminator instead of a newline."
    },
    {
      "predicate": {
        "operator": "exists",
        "operands": ["-b"]
      },
      "pclass": "pure",
      "inputs": ["stdin"],
      "outputs": ["stdout"],
      "true_str": "sha256sum -b",
      "comments": "Reads in binary mode, outputs checksum with '*' indicating binary."
    },
    {
      "predicate": {
        "operator": "exists",
        "operands": ["--help"]
      },
      "pclass": "pure",
      "inputs": [],
      "outputs": ["stdout"],
      "true_str": "sha256sum --help",
      "comments": "Displays help information."
    },
    {
      "predicate": {
        "operator": "exists",
        "operands": ["--version"]
      },
      "pclass": "pure",
      "inputs": [],
      "outputs": ["stdout"],
      "true_str": "sha256sum --version",
      "comments": "Displays version information."
    },
    {
      "predicate": {
        "operator": "exists",
        "operands": ["--tag"]
      },
      "pclass": "pure",
      "inputs": ["stdin"],
      "outputs": ["stdout"],
      "true_str": "sha256sum --tag",
      "comments": "Outputs a BSD-style checksum."
    },
    {
      "predicate": {
        "operator": "exists",
        "operands": ["--ignore-missing"]
      },
      "pclass": "pure",
      "inputs": [],
      "outputs": ["stderr"],
      "true_str": "sha256sum --ignore-missing",
      "comments": "The --ignore-missing option is meaningful only when verifying checksums."
    },
    {
      "predicate": {
        "operator": "exists",
        "operands": ["--quiet"]
      },
      "pclass": "pure",
      "inputs": [],
      "outputs": ["stderr"],
      "true_str": "sha256sum --quiet",
      "comments": "The --quiet option is meaningful only when verifying checksums."
    },
    {
      "predicate": {
        "operator": "exists",
        "operands": ["--strict"]
      },
      "pclass": "pure",
      "inputs": [],
      "outputs": ["stderr"],
      "true_str": "sha256sum --strict",
      "comments": "The --strict option is meaningful only when verifying checksums."
    },
    {
      "predicate": {
        "operator": "exists",
        "operands": ["-w"]
      },
      "pclass": "pure",
      "inputs": [],
      "outputs": ["stderr"],
      "true_str": "sha256sum -w",
      "comments": "The --warn option is meaningful only when verifying checksums."
    },
    {
      "predicate": {
        "operator": "len_args_eq",
        "operands": [1]
      },
      "pclass": "pure",
      "inputs": ["file"],
      "outputs": ["stdout"],
      "true_str": "sha256sum [FILE]",
      "comments": "Reads from a file and writes the SHA256 checksum to stdout."
    }
  ],
  "options": [
    "-c",
    "--status",
    "-z",
    "-b",
    "--help",
    "--version",
    "--tag",
    "--ignore-missing",
    "--quiet",
    "--strict",
    "-w"
  ]
}
