{
  "command": "cat",
  "cases": [
    {
      "predicate": "default",
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat",
      "comments": "The default behavior of cat reads from stdin and writes to stdout."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "-b"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat -b",
      "comments": "The -b flag numbers non-empty output lines."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "-n"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat -n",
      "comments": "The -n flag numbers all output lines."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "-T"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat -T",
      "comments": "The -T flag displays TAB characters as ^I."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "-A"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat -A",
      "comments": "The -A flag is equivalent to -vET."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "-E"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat -E",
      "comments": "The -E flag displays $ at the end of each line."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "-v"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat -v",
      "comments": "The -v flag uses ^ and M- notation, except for LFD and TAB."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "-t"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat -t",
      "comments": "The -t flag is equivalent to -vT."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "-e"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat -e",
      "comments": "The -e flag is equivalent to -vE."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "-s"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat -s",
      "comments": "The -s flag suppresses repeated empty output lines."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "-u"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "stdin"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat -u",
      "comments": "The -u flag is ignored."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "--help"
        ]
      },
      "pclass": "stateless",
      "inputs": [],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat --help",
      "comments": "The --help flag displays help information."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "flag",
          "--version"
        ]
      },
      "pclass": "stateless",
      "inputs": [],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat --version",
      "comments": "The --version flag displays version information."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "arg_type",
          "file"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "file"
      ],
      "outputs": [
        "stdout"
      ],
      "true_str": "cat <file>",
      "comments": "Reads from a file and writes to stdout."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "arg_type",
          "directory_empty"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "directory_empty"
      ],
      "outputs": [
        "stderr"
      ],
      "true_str": "cat <directory_empty>",
      "comments": "Returns an error when trying to read an empty directory."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "arg_type",
          "directory_nonempty"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "directory_nonempty"
      ],
      "outputs": [
        "stderr"
      ],
      "true_str": "cat <directory_nonempty>",
      "comments": "Returns an error when trying to read a non-empty directory."
    },
    {
      "predicate": {
        "operator": "eq",
        "operands": [
          "arg_type",
          "nonexistent"
        ]
      },
      "pclass": "stateless",
      "inputs": [
        "nonexistent"
      ],
      "outputs": [
        "stderr"
      ],
      "true_str": "cat <nonexistent>",
      "comments": "Returns an error when trying to read a nonexistent file or directory."
    }
  ],
  "options": [
    "-b",
    "-n",
    "-T",
    "-A",
    "-E",
    "-v",
    "-t",
    "-e",
    "-s",
    "-u",
    "--help",
    "--version"
  ]
}
