Underline tab row for switching views. Optional mono \`count\` per tab.

\`\`\`jsx
<Tabs tabs={[{id:'runs',label:'Runs',count:12},{id:'compare',label:'Comparison'}]} active={t} onChange={setT} />
\`\`\`