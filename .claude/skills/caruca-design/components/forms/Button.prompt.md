Primary action button — use for any clickable action; labels are sentence case, never uppercase.

\`\`\`jsx
<Button variant="primary" size="md" onClick={run}>Run comparison</Button>
\`\`\`

Variants: primary (blue), secondary (outlined), ghost (text, blue), danger. Sizes sm/md/lg. Pass \`icon\` for a leading 16px icon.